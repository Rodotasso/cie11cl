% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clusters.R
\name{cie11_validate_cluster}
\alias{cie11_validate_cluster}
\title{Validar clusters CIE-11 (post-coordinacion) / Validate ICD-11 clusters}
\usage{
cie11_validate_cluster(cluster)
}
\arguments{
\item{cluster}{Vector de caracteres; cada elemento es una expresion CIE-11,
posiblemente post-coordinada (p. ej. \code{"AB00.0&XA01"}). /
Character vector; each element is a possibly post-coordinated ICD-11
expression.}
}
\value{
Un data frame con una fila por expresion: \code{cluster}, \code{valid}
(logico), \code{n_components} (entero) y \code{reason} (motivo cuando no es valido). /
A data frame with one row per expression: \code{cluster}, \code{valid},
\code{n_components} and \code{reason}.
}
\description{
Valida de forma determinista expresiones de \strong{codificacion en cluster} de
CIE-11: combinaciones de codigos unidas por los conectores \code{&} (un stem con
sus ejes de post-coordinacion) y \code{/} (varios stems que juntos describen un
evento). La validacion es estructural y no depende del servidor de la OMS:
}
\details{
Deterministically validates ICD-11 \strong{cluster coding} expressions:
combinations of codes joined by \code{&} (a stem plus its post-coordination axes)
and \code{/} (several stems describing one event). Structural validation, with no
dependency on the WHO server:

\enumerate{
\item Todos los componentes deben existir en la fuente cargada. /
Every component must exist in the loaded source.
\item Un codigo de extension (Capitulo X, prefijo \code{X}) no puede actuar como
stem (no puede ir solo ni liderar un grupo separado por \code{/}). /
An extension code (Chapter X, prefix \code{X}) cannot act as a stem.
\item Un stem con ejes unidos por \code{&} debe admitir post-coordinacion
(\code{postcoordinationScale} no vacio). /
A stem joined by \code{&} must declare post-coordination.
}
}
\examples{
cie11_load()
cie11_validate_cluster(c("AB00.0&XA01", "AA00&XA01", "AB00/AC00", "XA01"))
}
