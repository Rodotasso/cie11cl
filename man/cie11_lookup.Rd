% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lookup.R
\name{cie11_lookup}
\alias{cie11_lookup}
\title{Buscar entidades CIE-11 por codigo / Look up ICD-11 entities by code}
\usage{
cie11_lookup(code)
}
\arguments{
\item{code}{Vector de caracteres con codigos CIE-11 (p. ej. \code{"1A00"}). /
Character vector of ICD-11 codes.}
}
\value{
Un data frame con una fila por codigo consultado, en el orden de
\code{code}; los codigos no encontrados devuelven una fila de \code{NA}. /
A data frame with one row per queried code, preserving input order; codes
not found yield a row of \code{NA}s.
}
\description{
Busqueda exacta y determinista de uno o mas codigos CIE-11 contra la fuente
de datos cargada (ver \code{\link[=cie11_load]{cie11_load()}}).
}
\details{
Deterministic exact lookup of one or more ICD-11 MMS codes against the
currently loaded data source.
}
\examples{
cie11_load()
cie11_lookup("AA00")
cie11_lookup(c("AA00", "ZZ99"))
}
